services:
  home-netflix:
    build: .
    ports:
      - "8000:8000"
    # Telegram creds for get_new_url() — set these in a .env file next to this
    # compose file (TG_API_ID, TG_API_HASH, TG_SESSION, TG_CHANNEL).
    env_file:
      - .env
    volumes:
      - ./Movies:/app/Movies
      - ./Covers:/app/Covers
      - ./data:/app/data
      - ./backend:/app/backend
      - ./frontend:/app/frontend
      - ~/Videos:/root/Videos
      - type : bind
        source : /media
        target : /media
        bind:
          propagation : rshared